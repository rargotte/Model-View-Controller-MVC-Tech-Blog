<section class="card">
  <a href="/comment/{{id}}">
    <header>{{content}}</header>

    <div class="card-body">
      <!--      <img src="/images/{{filename}}" alt="{{description}}">  -->
      <p>By: {{user_id}}</p>
    </div>
  </a>
</section>